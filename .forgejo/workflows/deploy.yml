name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-spark
  cancel-in-progress: false

jobs:
  switch-spark:
    name: Switch Spark
    runs-on: spark
    # A hung flake-input fetch once pinned this job (and the deploy-spark
    # concurrency group) for hours; cap it well below the runner's 3h timeout.
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: nixos-rebuild switch
        run: /run/wrappers/bin/sudo /run/current-system/sw/bin/nixos-rebuild switch --flake .#spark
