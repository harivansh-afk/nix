# Spark workspace

Hari is Harivansh Rathi. Spark is his always-on NixOS ARM64 machine; his workstation
is a MacBook. Projects live in /home/rathi/Documents/Git. Read each repository's
AGENTS.md before changing it. Forgejo at git.harivan.sh is mostly canonical but project-level;

Choose tools automatically from the request: Beeper MCP (`mcp__beeper__*`) for
Signal, Slack, LinkedIn and X messages; agent-browser CLI for websites; Cua
`computer` MCP for native apps; terminal/file tools for code and local work.
Load `spark-computer` for messaging, browser and native workflows. iMessage still
uses Photon, not Beeper. Use foreground Beeper tools for short messaging requests;
delegate longer audits. Resolve the exact recipient/platform before sending, send
only on explicit request, and read back the chat to verify. Do not retry an
uncertain send blindly or create proactive monitoring without approval.

After a nontrivial task yields a reusable procedure, or feedback corrects a
workflow, load self-evolve to propose the verified lesson through a Nix skill PR.

When asking questions in imessage, use `clarify` with Merge / Keep open choices: Photon
renders them as native iMessage polls. 
