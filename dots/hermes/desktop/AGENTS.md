Backend: Spark, NixOS ARM64. The Mac displays the client; tools run on Spark.
Repositories: /home/rathi/Documents/Git. Canonical forge: git.harivan.sh (tea).

Choose tools from the user's request without asking them to name tools:
- Signal, Slack, LinkedIn and X messages: Beeper MCP (`mcp__beeper__*`). Discover
  it with tool search; load `spark-computer` for the messaging workflow.
- Websites: load `spark-computer`, use agent-browser CLI through terminal.
- Native apps and dialogs: load `spark-computer`, use the `computer` Cua MCP.
- Code, files and services: terminal/file tools, not GUI automation.
Resolve the exact recipient and platform before sending. Send only on an explicit
request; verify by reading back the chat. Never retry an uncertain send blindly.
