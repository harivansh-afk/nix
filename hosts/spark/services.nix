{ ... }:
{
  # Stub for future homelab services (caddy, vaultwarden, ollama,
  # vllm via services.vllm.instances, betternas, etc). The upstream
  # dgx-spark module already exposes services.dgx-dashboard (on :11000)
  # and services.vllm.* — enable them here when ready.
}
