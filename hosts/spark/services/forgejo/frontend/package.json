{
  "name": "harivan-forgejo-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/harivan-forgejo.js --bundle --format=esm --target=es2022 --outdir=dist --entry-names=[name] --chunk-names=chunks/[name]-[hash] --splitting --minify"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
